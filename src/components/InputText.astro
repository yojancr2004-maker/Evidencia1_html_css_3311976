---
//Definimos las propiedades
//que pueda recibir el componente
interface Props {
    etiqueta: string;
    tipo: string;
}
//crear una variable
//para recibir la etiqueta
//Astro.props=Los valores desde
//            el control
const  etiqueta =Astro.props.etiqueta;
---
<!--
Etiqueta input:
se utiliza para los siguientes
controles:
- campo de texto
- password
- checkbox
- radio button
- boton de envio
ES ETIQUETA CERRADA
--->
<label> {etiqueta}:</label>
<input ="{type}"/>